#include "main.h"

/**
 * main - prints _putchar and ends in a new line
 *
 * Return: return 0 (Success)
 */
int main(void)
{
	int i;

	char p[10] = "_putchar"

	for (i = 0; i < 8; i++)
        {
	        _putchar(p[i]);
	}
        _putchar(10);
	return (0);
}	
